#include <stdlib.h>

#include <stdio.h>

#include <time.h>
/**
 * main - Entry Point
 *
 * Description: Print Single digit numbers
 *
 * Return: Always 0 (Sucess)
 */
int main(void)
{
int num;
for (num = 0; num < 10; num++)
{
printf("%d", num);
}
printf("\n");
return (0);
}
