#include <stdio.h>

#include<stdlib.h>

#include<time.h>
/**
 * main -Entry point
 *
 * Return: Always 0 (Sucess)
 */
int main(void)
{
int alp;
for (alp = 48; alp < 58; alp++)
{
putchar(alp);
}
putchar(10);
return (0);
}
